// Fragment Shader
uniform sampler2D u_Texture;

varying vec2 v_TexCoord;

void main() {
    vec4 texColor = texture2D(u_Texture, v_TexCoord);

    gl_FragColor = vec4(v_TexCoord, 0.0, 1.0); // Outputs UV as color;
}